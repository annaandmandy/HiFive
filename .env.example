# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here

# OpenAlex API Configuration (optional - for polite pool access)
OPENALEX_EMAIL=your_email@example.com

# Note: The application will work in mock mode without these API keys
# Mock data will be used automatically if API keys are not configured
